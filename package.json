{
  "name": "ollagram",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "prepare": "bash .github/setup.hooks.sh",
    "compile": "run-p build:*",
    "build:tsc": "bun tsc --noEmit",
    "build:test": "AGENT=1 bun test",
    "build:lint": "bun oxlint"
  },
  "dependencies": {
    "@effect/ai": "^0.35.0",
    "@effect/ai-openai": "^0.39.2",
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.89.0",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "npm-run-all": "^4.1.5",
    "oxfmt": "^0.49.0",
    "oxlint": "^1.64.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
